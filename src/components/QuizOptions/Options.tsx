import { Button } from "../ui/button"

function Options() {
    return (
        <div>
            <Button 
                className="w-10 h-5 text-center"
                variant={'default'}

            ></Button>
        </div>
    )
}

export default Options